Faça uma API usando express, para controle de bloqueio de CPF.

POST /deny-list/cpf
    Bloqueia o CPF - OK
DELETE /deny-list/cpf
    Libera o CPF da lista - OK
GET /deny-list/cpf                  
    Checa se o CPF esta bloqueado - OK
GET /status
    Retorna o status do servidor
        - A quanto tempo o servidor esta rodando; -- OK
        - A quantidade de consultas realizadas desde quando ele esta rodando; -- OK
        - A quantidade de CPF's na deny list; -- OK

OBSERVAÇÔES:

Os retornos desta API precisam ser no formato JSON. - ok 
Deve-se usar MongoDB para o armazenamento da deny list. - ok 
Deve-se validar se o CPF é válido em cada operação que ele for informado (11 digitos e apenas números) - Ok
Não é necessário suportar máscara (123.132.321-62), porém é um plus - Not ok
Faça um novo projeto para este exercício - ok 

-

Corrigir os bugs 


----

Não use o any, de forma alguma, nem desabilite 

Não utilizar o 'as' pois ignora propriedades obrigatorias, se utilizar, utilize da forma correta. Apenas qnd tiver certeza se ta certo. 

Quando a lib estiver com o DT, instale a lib do fim da mensagem

1 renomear tudo pra TS
2 tipar de dentro pra fora, das funções ate chegar no express

